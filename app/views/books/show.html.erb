<!DOCTYPE HTML>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
  </head>

  <body>
    <% if flash[:success] %>
      <p class="notice">
        <%= flash[:success] %>
      </p>
    <%end %>
    <p>
      <strong>Title: </strong>
      <%= @book.title %>
    </p>
    <p>
      <strong>Body: </strong>
      <%= @book.body %>
    </p>
    <%= link_to "Edit", edit_book_path(@book.id) %>
    |
    <%= link_to "Back", books_path %>
  </body>
</html>
